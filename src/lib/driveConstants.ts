/**
 * Google Drive integration constants.
 * Change the root folder name here — it will be reflected across the app.
 * NOTE: The same constant is duplicated in supabase/functions/_shared/drive-constants.ts
 *       for edge functions. Update both files when changing values.
 */

export const DRIVE_ROOT_FOLDER_NAME = "TAXBEBO";
